<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= dog.name %> </title>
</head>
<body>
<H1>Update <%= dog.name %> </H1>
<form action="/dogs/<%=dog._id%>?_method=PUT" method="POST">
    <label for="name">Name:</label>
    <input type="text" name="name" value="<%= dog.name %>"/>
    <br><br>
    <label for="breed">Breed:</label>
    <input type="text" name="breed" value="<%= dog.breed %>" />
    <br><br>
    <label for="rescueDog">Is a Rescue ?</label>
    <input type="checkbox" name="rescueDog" <% if (dog.rescueDog) {%> checked <%} %> >
    <br><br>
    <label for="pupsonality">Pupsonality:</label>
    <textarea name="pupsonality" id="pupsonality"></textarea>
    <br><br>
    <button type="submit">Pupdate</button>
</form>
</body>
</html>
